import styled from 'styled-components/macro';
import P from '../Pill';

export const List = styled.ul`
  margin: 0;
  padding: 0;
  list-style: none;
`;

export const Pill = styled(P)`
`;

export const Pointer = styled.span``;

export const TacticsWrapper = styled.div``;
